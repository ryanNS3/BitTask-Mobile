import { StyleSheet } from "react-native";
import { Background } from "../../../../global/colors";

export const styles = StyleSheet.create({
    buttonMode:{
        padding: 11,
        backgroundColor : Background.azBack8.backgroundColor,
        borderRadius: 5
    }
})